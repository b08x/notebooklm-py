interface:
  display_name: "Notebooklm Py"
  short_description: "Repo-specific patterns and workflows for notebooklm-py"
  default_prompt: "Use the notebooklm-py repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true